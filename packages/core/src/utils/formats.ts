/**
 * Format validators for JSON Schema
 *
 * Note: AJV with ajv-formats handles standard format validation automatically
 * for formats like 'date', 'date-time', 'email', 'uri', 'uuid', etc.
 *
 * This file is kept as a placeholder for any custom format validators that
 * may be needed in the future. It's imported for side effects in the main
 * entry point but currently provides no additional functionality.
 */

// No custom format validators needed - AJV with ajv-formats handles standard formats
