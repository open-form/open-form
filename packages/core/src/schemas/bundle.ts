// Re-export types from @open-form/types
// Note: OpenFormModule and OpenForm are no longer exported from @open-form/schemas
// The bundled schema (open-form.schema.json) is now the source of truth
export type { OpenFormPayload } from '@open-form/types';
