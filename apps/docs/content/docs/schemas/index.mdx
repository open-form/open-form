---
title: Introduction
description: OpenForm schema reference
---
OpenForm is built on JSON Schema standard [Draft 2020-12](https://json-schema.org/specification).


# Installation

The schemas are available as a bundle and as individual schemas.

# Hosted server

| Schema           | URL                                                 |
| ---------------- | --------------------------------------------------- |
| Latest bundle    | `https://schema.open-form.dev/schema.json`          |
| Versioned bundle | `https://schema.open-form.dev/2026-01-01.json`      |
| Individual items | `https://schema.open-form.dev/2026-01-01/form.json`, `bundle.json`, `checklist.json`, `document.json` |

# Package dependency

**Installation**

<Tabs items={['npm', 'pnpm', 'yarn', 'bun']} groupId="package-manager" persist>
  <Tab value="npm">
    ```bash
    npm install @open-form/schemas
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm add @open-form/schemas
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add @open-form/schemas
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun add @open-form/schemas
    ```
  </Tab>
</Tabs>

**Usage**

```typescript
import bundledSchema from '@open-form/schemas/schema.json'
```

# Schema Reference

The OpenForm schemas are broken down into 2 categories:


| Category                           | Description                                                                                                                         |
| ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| [Artifacts](/schemas/artifacts)    | Top-level artifact types that define document structures.                                                                           |
| [Primitives](/schemas/primitives)  | Low-level types for representing common data types like addresses, monetary amounts, and more. |
