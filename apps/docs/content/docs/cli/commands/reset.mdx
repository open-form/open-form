---
title: reset
description: Reset CLI to factory defaults
ogTitle: ofm reset
ogDescription: Reset the OpenForm CLI to factory default settings
---

Reset the OpenForm CLI to factory defaults.

# Usage

```bash
ofm reset [options]
```

# Options

| Option | Description |
|--------|-------------|
| `-y, --yes` | Skip confirmation prompt |
| `--keep-registries` | Keep registry configurations |
| `--keep-cache` | Keep cached data |

# Description

Performs a factory reset of the CLI by:

1. Clearing the registry cache (`~/.open-form/cache`)
2. Removing all installed renderer plugins (`~/.open-form/renderers`)
3. Creating a fresh default configuration (`~/.open-form/config.json`)

You will be prompted for confirmation unless `--yes` is passed.

# Examples

Full reset:

```bash
ofm reset -y
```

Reset but keep registries:

```bash
ofm reset --keep-registries
```

Reset but keep cache:

```bash
ofm reset --keep-cache
```

# Output

```
OpenForm CLI Reset

This will:
  • Create fresh default config (~/.open-form/config.json)
    - Registry configurations will be removed
  • Clear cache (~/.open-form/cache) - 45.2 KB
✓ Cleared 12 cached entries
✓ Cleared installed renderers
✓ Created default configuration

✓ OpenForm CLI has been reset to factory defaults

Default settings:
  Output format: json
  Artifacts directory: artifacts
  Cache TTL: 3600s (1 hour)
  Cache directory: ~/.open-form/cache
  Telemetry: enabled
```

# See also

- [configure](/cli/commands/configure) - Interactive configuration wizard
- [cache](/cli/commands/cache) - Manage registry cache
- [renderers](/cli/commands/renderers) - Manage renderer plugins
