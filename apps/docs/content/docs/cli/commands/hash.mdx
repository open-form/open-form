---
title: hash
description: Compute file checksums
ogTitle: ofm hash
ogDescription: Compute SHA256 checksums for use in OpenForm layer definitions
---

Compute a SHA256 checksum of a file, formatted for use in layer definitions.

# Usage

```bash
ofm hash <file> [options]
```

# Arguments

| Argument | Description |
|----------|-------------|
| `file` | File to compute hash for, or `-` for stdin |

# Options

| Option | Description |
|--------|-------------|
| `--json` | Output as JSON |
| `-a, --algorithm <algorithm>` | Hash algorithm (default: `sha256`) |

# Description

Computes the SHA256 checksum of a file and outputs it in the `sha256:<hex>` format used by OpenForm layer definitions. This is useful when manually adding file-backed layers to artifacts.

# Examples

Hash a PDF file:

```bash
ofm hash lease-template.pdf
```

Output:

```
sha256:a3f2b8c4e5d6...
```

JSON output:

```bash
ofm hash lease-template.pdf --json
```

```json
{
  "checksum": "sha256:a3f2b8c4e5d6...",
  "algorithm": "sha256",
  "hash": "a3f2b8c4e5d6...",
  "path": "lease-template.pdf"
}
```

Hash from stdin:

```bash
cat file.pdf | ofm hash -
```

# See also

- [fix](/cli/commands/fix) - Auto-compute checksums for all layers in an artifact
- [attach](/cli/commands/attach) - Attach files to artifacts as layers
