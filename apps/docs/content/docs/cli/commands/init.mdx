---
title: init
description: Initialize a new OpenForm project
ogTitle: ofm init
ogDescription: Create a new OpenForm project with the CLI
---

Initializes a new OpenForm project in the current or specified directory.

# Usage

```bash
ofm init [directory] [options]
```

# Arguments

| Argument | Description |
|----------|-------------|
| `directory` | Directory to initialize (defaults to current directory) |

# Options

| Option | Description |
|--------|-------------|
| `-y, --yes` | Non-interactive mode (skip prompts) |
| `--name <name>` | Project title (required in non-interactive mode) |
| `--description <desc>` | Project description |
| `--visibility <public\|private>` | Project visibility (default: private) |
| `--nested` | Allow creating nested project inside existing project |
| `--dry-run` | Preview without creating files |

# Description

The `init` command creates a new OpenForm project with the following structure:

```
my-project/
├── open-form.json      # Project manifest
└── .ofm/               # CLI state directory
    ├── HEAD
    ├── index.json
    ├── config.json
    ├── commits/
    └── objects/
```

In interactive mode, you'll be prompted for:
- Project title
- Description (optional)
- Visibility (public or private)
- Directory name

# Examples

Interactive initialization:

```bash
ofm init
```

Non-interactive with all options:

```bash
ofm init --yes --name "My Project" --visibility private
```

Initialize in a specific directory:

```bash
ofm init my-new-project
```

Preview what would be created:

```bash
ofm init --dry-run --yes --name "Test Project"
```

# See also

- [Projects](/cli/projects) - Project structure and manifest
- [add](/cli/commands/add) - Add artifacts to your project
