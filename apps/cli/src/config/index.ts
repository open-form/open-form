/**
 * Global CLI configuration constants
 */

/**
 * Console configuration
 */
export interface ConsoleConfig {
	baseUrl: string;
}

export const consoleConfig: ConsoleConfig = {
	baseUrl: 'https://open-form.dev',
};
